**What & when:** Generic content container — empty states, summary boxes, login card, anything that needs a raised surface.

```jsx
<Card>
  <h2>Library is Empty</h2>
  <p className="text-sub">Import your Fusion library or add tools manually.</p>
</Card>
```

For a collapsible titled section use `Panel`. For a clickable tool use `ToolCard`.
